---
description: "Generate commit message. Use when: write git commit, conventional commit, commit changes."
agent: "agent"
argument-hint: "Optional: Scope or type hint"
---

Use the skill `commit-message` to generate a Conventional Commit Message for the currently staged changes.
